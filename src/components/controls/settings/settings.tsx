import { h } from "preact";
import styles from "./settings.styles.css";

export default function Settings() {
  return (
    <div className={styles.settings}>
      <h3>Settings</h3>
      <p>settings prototype</p>
    </div>
  );
}
