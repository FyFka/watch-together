{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**"]
    },
    "lint": {
      "cache": false
    },
    "dev": {
      "cache": false
    }
  },
  "globalEnv": [".env", "PREACT_DEV_API_URL", "DATABASE_URI", "JWT_SECRET", "PORT"]
}
